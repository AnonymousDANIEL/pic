# Default asset slot

Optional files: `company-logo.png`, `game-left.png`, `game-right.png`, `person.png`, `background.jpg`, and `bank-bar.png`.
The app uses canvas placeholders when any file is absent.
